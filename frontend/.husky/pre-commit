#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd frontend || exit 1

if [ -n "$COMSPEC" ] && [ -x "$COMSPEC" ]; then
  WIN_PWD=$(pwd -W 2>/dev/null || pwd)
  "$COMSPEC" /d /c "cd /d \"$WIN_PWD\" && npm run test"
else
  npm run test
fi
